#Rename this to extra_model_paths.yaml and ComfyUI will load it

#config for a1111 ui
#all you have to do is change the base_path to where yours is installed
a111:
    base_path: /workspace/

    #checkpoints: models/Stable-diffusion
    #checkpoints: models
    #configs: models/Stable-diffusion
    vae: models
    loras: |
         models
         models
    upscale_models: |
                  models
                  models
                  models
    embeddings: models
    hypernetworks: models
    controlnet: models
#    custom_nodes: custom_nodes
    unet: models
comfyui:
     base_path: /workspace
#     checkpoints: models
#     checkpoints: models
     checkpoints: models
     clip: models
     clip_vision: models
     configs: models
#     controlnet: models
#     embeddings: models
#     loras: models
#     upscale_models: models
#     vae: models
other_ui:
    base_path: /workspace/
    checkpoints: custom_nodes
#    gligen: models
    custom_nodes: custom_nodes
#    checkpoints: models/Stable-diffusion
